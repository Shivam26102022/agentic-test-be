services:
  # A Docker web service
  - type: web
    name: fastapi-be
    runtime: python
    plan: free
    autoDeploy: false
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn agentic_system.main:app --host 0.0.0.0 --port $PORT
